doctype html
html
    head
        title #{meta.title}
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" type="text/css" href=`/static/styles/parts/${styling}`)
        if(script)
            script(defer src=`/static/scripts/${script}`)
    body
        block content
        footer
            p Copyright &copy; 2020